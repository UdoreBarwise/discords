# Server
PORT=5000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=1

# Discord Bot Token is stored in the database (bot_config table)
# Use the API endpoint POST /api/bot/token to set it

# External APIs (add as needed)
# API_KEY=your_api_key_here

